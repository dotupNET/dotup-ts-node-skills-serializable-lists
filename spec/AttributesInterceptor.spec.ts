import { LoadAttributesInterceptor } from '../src/AttributesInterceptor';
import { } from '../src/Extensions';

describe('LoadAttributesInterceptor', () => {

  it('should create an instance', () => {
    const value = new LoadAttributesInterceptor();
    expect(value)
      .toBeTruthy();
  });

});
