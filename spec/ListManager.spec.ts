import { ListManager } from '../src/ListManager';

describe('ListManager', () => {

  it('should create an instance', () => {
    const value = new ListManager();
    expect(value)
      .toBeTruthy();
  });

});
